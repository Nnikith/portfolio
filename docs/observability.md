# Observability

v1 scaffold includes:

- Docker healthcheck for Postgres
- `make smoke` for basic health verification
- `make doctor` for diagnostics

Additions later (not implemented here):

- request logging and structured logs
- audit logging for admin actions
- error tracking (Sentry, etc.)
